"""Phase 4 narrative detection — Hebrew-aware headline embeddings + cache."""

from sentisense.embed.embeddings import embed_missing, ensure_table, load_embeddings

__all__ = ["embed_missing", "ensure_table", "load_embeddings"]
